spring:
    application:
        name: movie-genre-service

server:
    port: 8082
eureka:
    client:
        registerWithEureka: true
        fetchRegistry: true
        serviceUrl:
            defaultZone: http://localhost:8080/eureka/
    instance:
        hostname: localhost